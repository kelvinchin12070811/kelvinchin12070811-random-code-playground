import { GoogleSgininDirective } from './google-sginin.directive';

describe('GoogleSgininDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogleSgininDirective();
    expect(directive).toBeTruthy();
  });
});
